# Frontend

前端基于 React + Vite，负责：

- 同时展示多个币种的真实汇率与波动趋势
- 展示带币种标识的告警日志
- 配置多币种监控列表、每个币种的目标价和全局轮询间隔
- 配置 Telegram 到价通知参数

当前版本注意事项：

- 不再提供模拟模式
- 图表会合并后端持久化历史与当前会话内的新采样数据
- 仪表盘支持一屏显示多个监控卡片，并切换当前图表币种
- 开发环境下通过 Vite 代理把 `/api/*` 转发到 `http://localhost:3001`

启动方式请优先参考仓库根目录的 [README](../README.md)。
